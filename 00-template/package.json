{
	"name": "mcp-template",
	"version": "0.1.0",
	"private": true,
	"description": "Plantilla base para construir un servidor MCP en TypeScript.",
	"type": "module",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"test": "tsx --test tests/example.test.ts"
	},
	"devDependencies": {
		"tsx": "^4.19.2",
		"typescript": "^5.6.3"
	}
}
